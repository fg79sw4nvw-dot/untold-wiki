# ゲーム本体 実装状況

**実装照合日：2026-09-15**  
**マイルストーン体系更新：2026-09-16**  
**照合対象：** `fg79sw4nvw-dot/untold-game` / `main`  
**照合コミット：** `c2d6306d84ad561181d897797ca1da8e198d6af3`

このページは、ゲーム本体の**実装進捗だけ**を記録する管理ページです。ゲーム仕様そのものの根拠には使用しません。

- 仕様の確定・未確定は、話題ごとの個別正本と `pages/open-items.md` で判断する。
- `未実装`・`未接続` は、仕様上の `未確定` を意味しない。
- 実装状況を更新するときは、古い引き継ぎ文書だけでなく `fg79sw4nvw-dot/untold-game` の現行 `main` を直接照合する。
- このページの照合コミットとゲーム本体 `main` のHEADが異なる場合、このページは古い可能性があるものとして扱う。
- 開発マイルストーンは `pages/development/free-exploration-core-milestone.md` のD/Aトラックを正とする。

## 現在のアプリ側マイルストーン位置

2026-09-15照合コミットを、新しいアプリトラックへ当てはめると、現在は**A1「基本プレイ縦切り完成」の途中**です。

A1の一部であるゲーム開始導入、通常移動基盤、BOOK、No.01取得、No.55導入、記録チュートリアル入口まではかなり接続されています。

一方で、現行の明確な接続境界は**初回記録候補を選択する具体UIへ入る直前**であり、まだ以下を満たしていません。

- UNTOLDの基本ループを1系統、実プレイとして最後まで完走する。
- 依頼を開始から完了まで遊ぶ。
- 日付・時間・セーブ／再開を含む継続プレイを縦切り範囲で確認する。
- 導入終了後の通常探索までを連続プレイとしてつなぐ。
- iPhone実機でA1範囲全体を通し確認する。

したがって、**A1未達**として扱います。

設計側の活動中マイルストーンはD0「既存設計の統合・棚卸し」です。設計と実装は別トラックなので、D0中でも、既に確定済みの仕様については実装を進めて構いません。

## 現在の実装到達点

### 1. 実画面で連続して動く序盤導入

現行 `main` では、少なくとも以下が実画面上で接続されています。

`冒頭独立映像 → エルド図書館開始イベント → BOOK発見・取得 → BOOK初回閲覧 → 司書への報告 → 図書館内の一時自由操作`

さらに、その後の以下も実装されています。

- No.01「忘れられた栞」の光表示・調査・カード化へ入る処理。
- No.01取得後の図書館退出接触から、No.55「書喰い鼠」Day 1導入へ入る処理。
- No.55導入会話後、初回記録チュートリアルの心の声までの状態遷移。

### 2. 司書報告

以前の進捗記録では停止位置未確定としていたが、現行正本では以下が確定済みで、ゲーム本体にも接続されています。

- 主人公停止位置：`(2,13)`。
- 主人公は左向き。
- 初回閲覧後の心の声終了から0.3秒後に強制移動開始。
- 停止0.3秒後に司書報告会話開始。
- BOOK受け渡し、司書の確認、主人公への観察、見送りまでを確定順序で進行。

仕様根拠は `pages/development/eld-opening-sequence.md` とする。

### 3. 図書館内の通常移動基盤

図書館の一時自由操作では、現行の通常操作基準に追従した基盤が接続されています。

- フローティング移動入力。
- 18px未満のリリースをタップ候補として扱う判定。
- 通常移動速度110px/s。
- 主人公の固定障害物用AABB：横24px × 縦16px。
- 軸ごとの衝突判定による壁沿いのスライド。
- 図書館出口への接触判定。
- 操作対象の方向・距離による選択基盤。
- デッドゾーン式カメラ基盤。

操作仕様の正本は `pages/systems/gameplay-ui.md` とする。

### 4. 実装済みの共通基盤

ゲーム本体には、現在の縦切り以外へ再利用できる以下の基礎モジュールが存在します。

- ゲーム内時計。
- 条件判定。
- 会話データ。
- 進行状態。
- 依頼追跡。
- 記録・記録フロー。
- 指定カード取得・所持状態。
- インベントリ。
- 装備。
- マップ遷移。
- 地形通行判定。
- ワールド表示基盤。

ただし、**モジュールが存在することと、完成版のゲームプレイへ接続済みであることは別**です。Aマイルストーン評価では、実際にプレイヤーが到達・操作できる接続状態を重視します。

## 現在の明確な接続境界

現行 `src/main.ts` の実画面進行は、No.55導入後の初回記録チュートリアルで、**記録候補を選択する具体UIへ入る直前**まで接続されています。

記録対象となる発言自体は確定している一方、現行実装では候補選択UIをそこで補完せず、確定済み境界で停止しています。

この境界より先を実装する場合も、実装都合で表示方式や数値を作らず、該当正本の確定範囲に従います。

## A1との差分

A1「基本プレイ縦切り完成」に対する現状は以下です。

| A1項目 | 現在の実装状態 |
|---|---|
| ゲーム開始から導入終了までの連続プレイ | 途中まで接続。初回記録UI手前が現行境界 |
| 通常移動 | 図書館内の基盤あり。完成版探索への接続は途中 |
| 会話・調査 | 序盤導入内で接続済み部分あり |
| BOOK | 初回取得・閲覧まで接続済み |
| 記録 | 基盤あり。初回具体選択UIより先は未接続 |
| カード化 | No.01で接続済み部分あり |
| 依頼 | No.55導入まで接続。完了までの連続プレイは未達 |
| マップ遷移 | 基盤モジュールあり。A1の通しプレイとしての確認は未完了 |
| 基本ループ1周 | 未達 |
| 日付・時間・セーブ／再開 | 基盤は一部存在するが、A1範囲の通し確認は未達 |
| iPhone実機でのA1通し確認 | 未達 |

## A2以降に残る主な範囲

A1達成後も、完成版に向けて少なくとも以下が残ります。

- D3で確定する主要システムを製品版の実ゲームへ統合する。
- 製品版の地域、町、主要施設、主要移動網を接続する。
- NPCの配置・時間帯・移動・会話を各地域へ統合する。
- 99枚の指定カードとNo.0関連要素を実装する。
- 依頼・イベント・会話・アイテム・装備・スペル・レシピ・ショップ等の製品版コンテンツを接続する。
- 開始から終盤・エンディングまでの全体プレイを成立させる。
- 自由探索順序、条件差、日跨ぎ、セーブ／再開を含む状態遷移を検証する。
- iPhone実機、PWA、パフォーマンス、UIを含む全体QAと仕上げを行う。
- D5「設計完成」後、A1〜A5を満たした状態でA6「リリース判定」を行う。

## CI確認

2026-09-15時点のゲーム本体では、現行の序盤導入・移動・No.01→No.55接続・初回記録チュートリアル基盤を含むテストとビルドが成功していることを確認済みです。

CI成功は仕様完成やゲーム完成を意味しません。現在実装されている範囲が壊れていないことの確認として扱います。

## 更新ルール

このページを更新するときは、次の順で確認します。

1. `untold-game/main` のHEADを確認する。
2. 実際の統合入口とプレイヤーが到達できる最遠地点を確認する。
3. 共通基盤の存在と、実ゲームへの接続状態を分けて記録する。
4. `pages/development/free-exploration-core-milestone.md` のAトラックとの差分を更新する。
5. Aマイルストーンを完了扱いする場合は、達成条件を1項目ずつ実ゲームで確認する。
6. 確定仕様の詳細をこのページへ複製しすぎず、個別正本へリンクする。

これにより、進捗ページを古い仕様要約や廃止マイルストーンの墓場にしない。
