# UNTOLD Wiki - AI Agent Entry Point

このファイルは、ChatGPT / Codex / その他のAIエージェント向けの**最小入口**です。ゲーム仕様そのものの正本ではありません。

## 通常の最短参照

1. この `AGENTS.md` を読む。
2. `ai-index.json` で話題に対応する正本を特定する。
3. **その作業に直接必要な正本だけ**を読む。

Wiki全体、`README.md`、`ai-manifest.json`、`pages/chatgpt-guide.md` を毎回読み直す必要はありません。

- 「現在のマイルストーン」「次に何を決めるか」「現在地」では、`ai-current.json` を入口にする。
- 開発再開時や「次は何を決めるか」を判断するときは、`ai-current.json` に加えて **`decision-inbox.md` の未統合確定事項も確認する**。未統合事項がある場合は、正本へまだ統合されていなくても有効な確定事項として扱う。
- 開発再開時や「次は何を決めるか」を判断するときは、`ai-current.json` だけで結論を出さず、**次候補の話題を `ai-index.json` で再ルーティングし、その個別正本を確認してから未確定判定する**。
- 進行が複数の正本へまたがる場合は、現在読んでいる正本から示された後続正本・関連正本をたどり、**最後に確定済みの進行地点まで確認してから**次の設計候補を出す。
- ルーティングが曖昧、複数ページの優先順位が必要、旧資料の扱いを確認したい場合だけ `ai-manifest.json` を読む。
- Wiki保守、監査、判断基準、参照ルール自体を変更する場合は `pages/chatgpt-guide.md` と `pages/governance/spec-maintenance.md` を読む。

## 絶対ルール

- `main` を正本として扱う。
- 未確定仕様を、自然さ・一般論・実装都合で補完しない。
- ただし、**イベント演出の細かい秒数、停止位置、向き、数px〜1タイル程度の配置調整など、既存の確定仕様と矛盾せず、物語・ゲーム性・取得条件・進行分岐等の意味を変えない微調整**については、ChatGPTが不自然さの出ない値を判断してその場で確定してよい。この裁量には、ストーリーの意味、主要システム、ゲームバランスへ影響する数値、取得条件、会話内容、進行分岐などは含めない。
- `確定` と明記された内容だけを確定仕様として扱う。`暫定` は暫定、`案`・`候補`・`未確定` は未採用。
- 同じ話題に個別の正本ページがある場合、`pages/development/implementation.md` や進捗ページより個別正本を優先する。
- **`decision-inbox.md` は正本ではないが、未統合の `確定` 項目は正本より後に確定した変更として扱う。** 通常の会話中の「反映」「記録」はここへ高速追記し、10項目到達・話題終了・正本統合の明示指示で正本へまとめて統合する。詳細は `pages/chatgpt-guide.md` を正とする。
- **Decision Inboxから正本への通常統合は超軽量で行う。** targetごとにまとめ、対象正本を原則1回だけ読み、指定sectionへまとめて反映する。通常統合ではWiki全体、`pages/open-items.md`、各索引・AIルーターの横断監査を行わない。構造変更・明白な矛盾・定期監査・ユーザーの監査指示がある場合だけ追加確認する。
- **`未実装`・`未接続`・`実装保留` は実装状態であり、仕様の `未確定` を意味しない。** 進捗ページやゲーム本体の欠落だけを根拠に「次に決める項目」と判断してはいけない。
- 「未確定」と判断する前に、`pages/open-items.md` だけでなく、その話題の個別正本を確認する。両者が食い違う場合は個別正本を優先し、未確定台帳や進捗要約を整理対象とする。
- `ai-index.json` と `ai-current.json` は**参照先を決めるための非正本キャッシュ**であり、具体仕様の根拠にはしない。
- `wiki.json`、旧マップ資料、履歴ページを現行仕様の根拠にしない。
- 実装状況はWikiだけで断定せず、必要に応じて `fg79sw4nvw-dot/untold-game` の現行 `main` を確認する。
- 新規公開ページ追加・削除時は `pages.json` も更新する。
- 仕様を確定・変更した場合の関連 `pages/open-items.md`、索引、名称参照への影響確認は、**通常の軽量統合では毎回行わず、構造変更・明白な矛盾・定期監査・ユーザー指示時に行う**。

詳細ルーティングは `ai-manifest.json`、Wiki保守ルールは `pages/chatgpt-guide.md` と `pages/governance/spec-maintenance.md` を正とします。
